name: "Archive Issues and Pull Requests"

on:
  schedule:
    - cron: '0 0 * * 0,2,4'
  repository_dispatch:
    types: [archive]
  workflow_dispatch:
    inputs:
      archive_full:
        description: 'Recreate the archive from scratch'
        default: false
        type: boolean

jobs:
  build:
    name: "Archive Issues and Pull Requests"
    runs-on: ubuntu-latest
    steps:
    - name: "Checkout"
      uses: actions/checkout@v4

    - name: "Update Archive"
      uses: martinthomson/i-d-template@7b8eb97cf0daa28cc14be5c87ec4c35b10577974
      env:
        ARCHIVE_FULL: ${{ inputs.archive_full }}
      with:
        make: archive
        token: ${{ github.token }}

    - name: "Update GitHub Pages"
      uses: martinthomson/i-d-template@7b8eb97cf0daa28cc14be5c87ec4c35b10577974
      with:
        make: gh-archive
        token: ${{ github.token }}

    - name: "Save Archive"
      uses: actions/upload-artifact@v4
      with:
        name: archive
        path: archive.json
